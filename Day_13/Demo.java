public class Demo {
    public static void main(String args[]){
        Demo d = new Demo();
        //there are default methods that are inherited form the OBJECT super class in java by the demo d object 
        //on putting the dot . operator after the demo class object d ,
        //We would be able to observe that many methods are available , 
        //these methods are non static methods inhertied by the DEMO class
        //from the OBJECT Super class by default by JVM and java.
        //The child class that inherits from Demo , will be able to inherit not only the parent's non static methods and attributes
        //But as well as the OBJECT Super class non static methods that are by default  
    }
}
